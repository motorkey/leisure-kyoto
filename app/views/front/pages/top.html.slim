- flash.each do |key, value|
  .alert class="alert-#{key}"
    = value
p When are you free?
= form_tag events_path, method: :get
  .form-group
    = text_field_tag :day, '', class: 'datepicker form-control', readonly: 'readonly', eequired: true
  .form-group
    = submit_tag 'Search', class: 'btn btn-default'
